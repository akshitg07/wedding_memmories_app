'use client'; import { useState } from 'react'; import { api } from '@/lib/api'; import { useRouter } from 'next/navigation';
export default function Login(){ const [username,setUsername]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const router=useRouter(); async function submit(e:React.FormEvent){ e.preventDefault(); setError(''); try{ const {data}=await api.post('/auth/login',{username,password}); localStorage.setItem('accessToken',data.accessToken); router.push('/gallery'); }catch{ setError('Invalid username or password'); }} return <main className="grid min-h-[calc(100vh-64px)] place-items-center px-4"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-8"><h1 className="text-3xl font-bold">Welcome back</h1><p className="mt-2 text-stone-600 dark:text-stone-300">Sign in to view and upload wedding memories.</p><input className="mt-8 w-full rounded-2xl border bg-white/70 p-3 dark:bg-black/30" placeholder="Username" value={username} onChange={e=>setUsername(e.target.value)}/><input className="mt-3 w-full rounded-2xl border bg-white/70 p-3 dark:bg-black/30" placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="mt-3 text-sm text-red-500">{error}</p>}<button className="mt-6 w-full rounded-2xl bg-ink p-3 text-white dark:bg-white dark:text-ink">Login</button></form></main> }
